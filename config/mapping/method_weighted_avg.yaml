# Weighted Average Method Configuration
# voxelmap_fusion style: P_new = (n*P_old + P_obs)/(n+1)
slam_node:
  ros__parameters:
    mapping_3d:
      intensity_threshold: 35     # Minimum intensity for observation
      # Intensity -> Probability mapping: [threshold, 255] -> [0.7, 0.95]
      # (max intensity and the 0.7 occupied cut are hardcoded in
      # mapping_3d.py - keys advertising them here were never read)
